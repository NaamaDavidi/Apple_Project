from Python.Training.functionExample.utils import utils


class homework():
    first_name = 'naama'
    last_name= 'davidi'
    first_name_length = utils.fins_len_of_word(first_name)
    last_name_length = utils.fins_len_of_word(last_name,'demo updated')